<Window x:Class="ToastDeckA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ToastDeck-A Demo"
        Height="560"
        Width="900"
        MinHeight="480"
        MinWidth="760"
        Background="#F5F7FA">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="TestNotificationButton"
                        MinWidth="148"
                        Height="36"
                        Margin="0,0,8,0"
                        Padding="14,0"
                        Click="TestNotificationButton_Click">
                    Send Windows Toast
                </Button>
                <Button x:Name="EnableWindowsListenerButton"
                        MinWidth="188"
                        Height="36"
                        Padding="14,0"
                        Click="EnableWindowsListenerButton_Click">
                    Retry Windows Capture
                </Button>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="ToastDeck-A Demo"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="#151A23" />
                <TextBlock Text="Background tray app with persistent always-on-top toast cards."
                           Margin="0,6,0,0"
                           FontSize="14"
                           Foreground="#5E6675" />
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1"
                Margin="0,22,0,16"
                Padding="16"
                Background="White"
                BorderBrush="#D7DCE5"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Listener status"
                               FontWeight="SemiBold"
                               Foreground="#151A23" />
                    <TextBlock x:Name="ListenerStatusText"
                               Margin="0,4,0,0"
                               Text="Starting Windows notification capture..."
                               Foreground="#5E6675"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Button Grid.Column="1"
                        MinWidth="128"
                        Height="34"
                        Margin="20,0,0,0"
                        Padding="12,0"
                        Click="ClearNotificationsButton_Click">
                    Clear List
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#D7DCE5"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="16,14,16,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Title" FontWeight="SemiBold" Foreground="#404756" />
                    <TextBlock Grid.Column="1" Text="Message" FontWeight="SemiBold" Foreground="#404756" />
                    <TextBlock Grid.Column="2" Text="Source" FontWeight="SemiBold" Foreground="#404756" Margin="16,0" />
                    <TextBlock Grid.Column="3" Text="Time" FontWeight="SemiBold" Foreground="#404756" />
                </Grid>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Notifications}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="16,12"
                                    BorderBrush="#EDF0F5"
                                    BorderThickness="0,1,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="3*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="{Binding Title}"
                                               FontWeight="SemiBold"
                                               Foreground="#151A23"
                                               TextWrapping="Wrap" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Message}"
                                               Foreground="#404756"
                                               TextWrapping="Wrap"
                                               Margin="16,0" />
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding Origin}"
                                               Foreground="#5E6675"
                                               Margin="16,0" />
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding CreatedAtLocalText}"
                                               Foreground="#5E6675" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>
