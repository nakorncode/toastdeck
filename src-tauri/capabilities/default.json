{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the ToastDesk overlay window",
  "windows": ["toast"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top"
  ]
}
